language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-themosis' ]; then cd .. && eval "mv $currentfolder generator-themosis" && cd generator-themosis; fi

